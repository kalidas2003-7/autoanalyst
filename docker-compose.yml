version: "3.8"
services:
  autoanalyst:
    build: .
    ports:
      - "8501:8501"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
      - ./sessions:/app/sessions
